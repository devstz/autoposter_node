# Autoposter Node Bot 🚀

Автоматический деплой и запуск Python-бота с поддержкой PostgreSQL, логированием и systemd-сервисом.

## ⚡ Быстрый старт

1. Убедитесь, что у вас есть **sudo**-доступ и установлен **Git**.
2. Выполните установку одной командой:

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/devstz/autoposter_node/main/install_autoposter.sh)" -- "ВАШ_TOKEN" "ВАШ_DATABASE_URL"
```

> ⚠️ замените `ВАШ_TOKEN` и `ВАШ_DATABASE_URL` на реальные значения вашего Telegram-бота и базы PostgreSQL.

## 🧰 Что делает скрипт

* Обновляет систему и устанавливает зависимости (`python3`, `pip`, `git`)
* Клонирует репозиторий `autoposter_node`
* Создаёт `.env` с вашими данными
* Разворачивает виртуальное окружение `.venv` и ставит зависимости
* Проверяет:

  * подключение к базе (`check_db.py`)
  * валидность токена бота (`check_bot.py`)
  * отсутствие дубликатов IP в таблице `bots` (`check_dublication_ip.py`)
* Если всё в порядке — создаёт systemd-сервис, запускает и проверяет его статус

## 🧾 Логи

Все логи установки сохраняются в `/var/log/autoposter_install.log`.
Логи работы бота — в `output.log` в корне проекта.

## 🛠 Управление сервисом

```bash
sudo systemctl status autoposter.service     # статус
sudo systemctl restart autoposter.service    # перезапуск
sudo systemctl stop autoposter.service       # остановка
sudo journalctl -u autoposter.service -f     # логи в реальном времени
```

## 🧹 Удаление

```bash
sudo systemctl stop autoposter.service
sudo systemctl disable autoposter.service
sudo rm -f /etc/systemd/system/autoposter.service
sudo rm -rf /opt/autoposter_node
```
